#!/usr/bin/env bash
# Env: MOCK_CSRUTIL=enabled|disabled (default: enabled)
case "${MOCK_CSRUTIL:-enabled}" in
  enabled)  echo "System Integrity Protection status: enabled." ;;
  disabled) echo "System Integrity Protection status: disabled." ;;
esac
